{
  "name": "claude-desktop-linux-shims",
  "version": "0.0.0",
  "private": true,
  "description": "Linux-compatible shims (native bindings + BrowserWindow frame patch) for repackaging Claude Desktop",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf build-ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
